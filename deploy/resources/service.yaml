apiVersion: v1
kind: Service
metadata:
  name: letssee666
spec:
  selector:
    app: letssee666
  ports:
  - name: web
    nodePort: 30084
    port: 80
    protocol: TCP
    targetPort: 80
  type: NodePort
